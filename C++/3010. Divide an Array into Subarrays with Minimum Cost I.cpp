class Solution {
public:
    int minimumCost(vector<int>& nums)
    {
        int result = 0;
        int i = 0;
        int n = nums.size();
        int prev = INT_MAX;

        for (; i < n - 2; ++i) {
            if (nums[i] >)

                prev = nums[i];
        }
    }
};
